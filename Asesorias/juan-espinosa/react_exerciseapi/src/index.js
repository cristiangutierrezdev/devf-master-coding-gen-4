//Crea un projecto de react donde hagas una peticion a la siguiente API https://picsum.photos/v2/list 
//muestra los resultados en las ruta /images 
//muestra los resultados utilizando useEffect, useState  y un componente llamado Card.js
//el componente debe mostrar como minimo La imagen(download_url) y el autor(author)

import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

ReactDOM.render(<App />,document.getElementById('root'));

